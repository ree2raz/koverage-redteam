.venv
__pycache__
*.pyc
*.pyo
.pytest_cache
*.egg-info

# Local secrets — never commit (holds MODAL_OSS_URL / API keys)
.env

# Local-only review/prep notes — kept on disk, not published to the public repo
REVIEW_GUIDE.md
PLAN_VS_DELIVERED.md
INTERVIEW_PRIMER.md

# Red-team run artifacts (transcripts + summaries are regenerated by `make attack`)
attack_out*/
attack_smoke/
# Archived attack runs: ignore the bulky per-run dirs + latest symlink, but KEEP
# the human-readable ledger (small, useful provenance) under version control.
attack_runs/*/
attack_runs/latest
